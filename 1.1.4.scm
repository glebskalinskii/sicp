#! /usr/local/bin/csi -script

(define (square x) (* x x))

(define (sum-of-squares x y)
  (+ (square x) (square y)))

(define (f a)
  (sum-of-squares (+ a 1) (* a 2)))


(print
;-----------------------------------------------------------

(square 4)

;-----------------------------------------------------------
)
(print
;-----------------------------------------------------------

(square (+ 2 3))

;-----------------------------------------------------------
)
(print
;-----------------------------------------------------------

(square (square 2))

;-----------------------------------------------------------
)
(print
;-----------------------------------------------------------

(sum-of-squares 4 3)

;-----------------------------------------------------------
)
(print
;-----------------------------------------------------------

(f 5)

;-----------------------------------------------------------
)
